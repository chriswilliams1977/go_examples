#struct = structure
#allows you to store mixed data types together e.g a user
#bit like an object
#array, slice and map values can only store same data type
#use type definition to create instance of that type to use